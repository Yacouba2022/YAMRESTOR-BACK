package yamrestor.restor.enums;

/** Destination d'une imprimante (poste d'impression). */
public enum TypeImprimante {
    TICKET_CLIENT,
    CUISINE,
    BAR,
    PATISSERIE,
    FACTURE,
    DEVIS
}
